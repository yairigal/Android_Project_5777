package project.android.com.second_app.model.backend;

/**
 * Created by Yair on 2017-01-18.
 */


/*
public class SeachFilter {
    String searchInput;
    ArrayList rawData;
    ArrayList output;

    
    public SeachFilter(String input,ArrayList raw){
        rawData = raw;
        searchInput = input;
    }


    private boolean TryParseAttribute(String i) {

    }


    private boolean TryParseRange(String i,ArrayList<Predicate<Input>> toReturn) throws Exception {
        try {
            String[] range = i.split("-");
            if(range.length > 2)
                throw new Exception("Error parsing");
            int low = Integer.parseInt(range[0]);
            int high = Integer.parseInt(range[1]);
            toReturn.add(new Predicate<Input>() {
                @Override
                public boolean Do(Input input) {
                }
            });
        }
        catch (Exception ex){
            throw ex;
        }

    }
}
*/
